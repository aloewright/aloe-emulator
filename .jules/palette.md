## 2025-05-20 - Icon-Only Button Accessibility
**Learning:** Icon-only buttons (like password toggles) must not have `tabindex="-1"` and must have an `aria-label`.
**Action:** Always verify keyboard navigability and screen reader support for helper buttons within inputs.
